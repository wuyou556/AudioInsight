# AudioInsight - Claude Configuration

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for this repository. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` at repo root + `docs/adr/` for architecture decisions. See `docs/agents/domain.md`.
